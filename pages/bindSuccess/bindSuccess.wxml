<view class="bind-success">
  <view class="img">
    <image src="/image/logo.png" mode="aspectFill"></image>
  </view>
  <view class="title">您通过 {{upName}} 的分享绑定成功</view>
</view>

<!-- 底部浮动栏目 -->
<view class="footer" style="height:{{iphoneFooter ? 153 : 115}}rpx">
  <view class="fbox">
    <view class="save" wx:if="{{isPromoter}}" bindtap="to" data-url="/pages/applyPromoter/applyPromoter">成为推广员</view>
    <view class="save" wx:else bindtap="to" data-url="/pages/promotionCode/promotionCode">我的推广码</view>
  </view>
</view>
<view style="height:{{iphoneFooter ? 153 : 115}}rpx;"></view>